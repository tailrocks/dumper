query userCurrent {
  userCurrent {
    data {
      username
      email
      firstName
      lastName
    }
    error {
      ... on SecurityError {
        code
        message
      }
    }
  }
}
